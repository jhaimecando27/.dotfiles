# aliases.sh

alias ls="ls -l --group-directories-first --color=always"
alias ll="ls -la --group-directories-first --color=always"

alias cp="cp -i"
alias mv="mv -i -v"
alias rm="rm -i"
alias mkdir="mkdir -pv"

alias vim="nvim"

alias open="xdg-open"

alias yta="youtube-dl --extract-audio --audio-format mp3"
alias ytv="youtube-dl -f bestvideo"
alias ytav="youtube-dl -f bestvideo+bestaudio"
